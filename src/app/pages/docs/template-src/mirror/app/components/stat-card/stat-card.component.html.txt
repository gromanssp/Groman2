<div class="stat-card glass-panel accent-{{ accent() }}">
  <div class="stat-header">
    <div class="stat-title">{{ title() }}</div>
    <div class="stat-icon-wrapper">
      <app-icon [path]="icon()" [size]="20" />
    </div>
  </div>

  <div class="stat-body">
    <div class="stat-value heading-2" [appSkeleton]="loading()">{{ value() }}</div>

    <div class="stat-trend" [class.trend-up]="trend() >= 0" [class.trend-down]="trend() < 0" [appSkeleton]="loading()">
      @if (trend() >= 0) {
        <svg fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
        </svg>
      } @else {
        <svg fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 17h8m0 0V9m0 8l-8-8-4 4-6-6" />
        </svg>
      }
      <span>{{ trend() > 0 ? '+' : '' }}{{ trend() }}%</span>
    </div>
  </div>
</div>
