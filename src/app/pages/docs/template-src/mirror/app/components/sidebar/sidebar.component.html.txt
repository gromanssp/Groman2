<aside class="sidebar h-100 d-flex flex-column" [class.collapsed]="collapsed()">
  <div class="sidebar-header">
    <div class="logo">
      <div class="logo-icon"></div>
      <span class="heading-3 logo-text">Groman<span class="text-gradient">2</span></span>
    </div>
    <button class="toggle-btn" type="button" (click)="toggle()" aria-label="Toggle sidebar">
      <svg fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 19l-7-7 7-7m8 14l-7-7 7-7" />
      </svg>
    </button>
  </div>

  <nav class="sidebar-nav">
    @for (group of groups; track group.title) {
      <div class="nav-group">
        <button
          class="nav-section-title"
          type="button"
          [attr.aria-expanded]="!isFolded(group.title)"
          (click)="toggleGroup(group.title)">
          <span>{{ group.title }}</span>
          <svg class="group-chevron" [class.folded]="isFolded(group.title)" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
          </svg>
        </button>

        @if (!isFolded(group.title)) {
          @for (item of group.items; track item.path) {
            <a
              class="nav-item"
              [routerLink]="item.path"
              routerLinkActive="active"
              [appTooltip]="collapsed() ? item.label : ''"
              tooltipPosition="right"
              (click)="closeMobile()">
              <span class="nav-icon">
                <svg fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" [attr.d]="item.icon" />
                </svg>
              </span>
              <span class="nav-label">{{ item.label }}</span>
            </a>
          }
        }
      </div>
    }
  </nav>

  <div class="sidebar-footer">
    <a routerLink="/auth/login" class="nav-item logout" [appTooltip]="collapsed() ? 'Logout' : ''" tooltipPosition="right">
      <span class="nav-icon">
        <svg fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
        </svg>
      </span>
      <span class="nav-label">Logout</span>
    </a>
  </div>
</aside>
