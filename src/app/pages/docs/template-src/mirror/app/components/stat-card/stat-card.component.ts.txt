import { ChangeDetectionStrategy, Component, input, numberAttribute } from '@angular/core';
import { SkeletonDirective } from '../../directives/skeleton.directive';
import { IconComponent } from '../../shared/components/icon/icon.component';

export type StatAccent = 'primary' | 'secondary' | 'success' | 'warning';

@Component({
  selector: 'app-stat-card',
  templateUrl: './stat-card.component.html',
  styleUrls: ['./stat-card.component.css'],
  imports: [SkeletonDirective, IconComponent],
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class StatCardComponent {
  readonly title = input('Statistic');
  readonly value = input<string | number>('0');
  /** SVG path data for the corner icon (see `ICONS` in the pages that use it). */
  readonly icon = input<string | readonly string[]>('');
  readonly trend = input(0, { transform: numberAttribute });
  readonly accent = input<StatAccent>('primary');
  readonly loading = input(false);
}
