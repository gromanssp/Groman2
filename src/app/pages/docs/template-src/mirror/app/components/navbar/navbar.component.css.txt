:host {
  display: block;
}

.navbar-left {
  display: flex;
  align-items: center;
  gap: 1rem;
}

.hamburger-btn {
  background: transparent;
  border: none;
  color: var(--navbar-text-secondary, var(--text-secondary));
  width: 40px;
  height: 40px;
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all var(--transition-fast);
}

.hamburger-btn:hover {
  background: var(--navbar-hover-bg, rgba(var(--overlay-rgb), 0.05));
  color: var(--navbar-text, var(--text-primary));
}

.hamburger-btn svg {
  width: 22px;
  height: 22px;
}

.navbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1rem 2rem;
  background: var(--navbar-bg, var(--bg-surface));
  backdrop-filter: var(--glass-blur);
  -webkit-backdrop-filter: var(--glass-blur);
  border-bottom: var(--navbar-border, var(--glass-border));
  height: 80px;
  position: relative;
  z-index: 50;
}



.search-icon {
  width: 18px;
  height: 18px;
  color: var(--navbar-text-muted, var(--text-muted));
  margin-right: 0.75rem;
}



.navbar-actions {
  display: flex;
  align-items: center;
  gap: 1.5rem;
}

.icon-btn {
  background: transparent;
  border: none;
  color: var(--navbar-text-secondary, var(--text-secondary));
  width: 40px;
  height: 40px;
  border-radius: var(--radius-round);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all var(--transition-fast);
  position: relative;
}

.icon-btn:hover {
  background: var(--navbar-hover-bg, rgba(var(--overlay-rgb), 0.05));
  color: var(--navbar-text, var(--text-primary));
}

.icon-btn svg {
  width: 20px;
  height: 20px;
}

.notification-badge {
  position: absolute;
  top: 8px;
  right: 8px;
  width: 8px;
  height: 8px;
  background: var(--danger);
  border-radius: 50%;
  box-shadow: 0 0 0 2px var(--navbar-bg, var(--bg-surface));
}

.user-profile {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  cursor: pointer;
  padding: 0.25rem 0.5rem;
  border-radius: var(--radius-lg);
  transition: background var(--transition-fast);
}

.user-profile:hover {
  background: var(--navbar-hover-bg, rgba(var(--overlay-rgb), 0.05));
}




.user-info {
  display: flex;
  flex-direction: column;
}

.user-name {
  font-size: 0.9rem;
  font-weight: 500;
  color: var(--navbar-text, var(--text-primary));
  margin: 0;
}

.user-role {
  font-size: 0.75rem;
  color: var(--navbar-text-muted, var(--text-muted));
  margin: 0;
}







/* Search trigger opens the command palette */
.search-trigger {
  display: flex;
  align-items: center;
  gap: 0.65rem;
  width: 320px;
  max-width: 40vw;
  padding: 0.5rem 1rem;
  background: var(--navbar-search-bg, rgba(var(--overlay-rgb), 0.03));
  border: 1px solid var(--navbar-search-border, rgba(var(--overlay-rgb), 0.08));
  border-radius: var(--radius-round);
  color: var(--navbar-text-muted, var(--text-muted));
  font-family: inherit;
  font-size: 0.9rem;
  cursor: pointer;
  transition: all var(--transition-fast);
}
.search-trigger:hover { border-color: var(--accent-primary); color: var(--navbar-text, var(--text-primary)); }
.search-placeholder { flex: 1; text-align: left; }
.search-kbd {
  padding: 0.1rem 0.4rem;
  background: rgba(var(--overlay-rgb), 0.08);
  border: 1px solid rgba(var(--overlay-rgb), 0.1);
  border-radius: 4px;
  font-family: inherit;
  font-size: 0.7rem;
}

.navbar-breadcrumbs { margin-left: 0.5rem; }

.notification-badge {
  position: absolute;
  top: 4px;
  right: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 16px;
  height: 16px;
  padding: 0 4px;
  background: var(--danger);
  border-radius: var(--radius-round);
  color: #fff;
  font-size: 0.65rem;
  font-weight: 600;
  box-shadow: 0 0 0 2px var(--navbar-bg, var(--bg-surface));
}

.user-profile {
  background: transparent;
  border: none;
  font-family: inherit;
  text-align: left;
}
.user-info { display: flex; flex-direction: column; }

/* Notification dropdown panel */
.notification-panel { display: flex; flex-direction: column; }
.notification-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0.5rem 0.65rem 0.6rem;
  border-bottom: 1px solid rgba(var(--overlay-rgb), 0.06);
  font-size: 0.8rem;
  font-weight: 600;
  color: var(--text-secondary);
}
.link-btn {
  background: none;
  border: none;
  color: var(--accent-primary);
  font-family: inherit;
  font-size: 0.75rem;
  cursor: pointer;
}
.notification-item {
  padding: 0.6rem 0.65rem;
  border-radius: var(--radius-sm);
  transition: background var(--transition-fast);
}
.notification-item:hover { background: rgba(var(--overlay-rgb), 0.05); }
.notification-item.unread { border-left: 2px solid var(--accent-primary); }
.notification-title { font-size: 0.85rem; color: var(--text-primary); }
.notification-time { font-size: 0.72rem; color: var(--text-muted); }
.notification-empty { padding: 1rem; text-align: center; color: var(--text-muted); font-size: 0.85rem; }

/* Command palette */
.command-backdrop {
  position: fixed;
  inset: 0;
  display: flex;
  justify-content: center;
  padding-top: 12vh;
  background: rgba(0, 0, 0, 0.55);
  backdrop-filter: blur(3px);
  z-index: var(--z-modal);
}

.command-palette {
  width: min(560px, 92vw);
  max-height: 60vh;
  display: flex;
  flex-direction: column;
  background: var(--bg-elevated);
  border: var(--glass-border-light);
  border-radius: var(--radius-lg);
  box-shadow: var(--glass-shadow);
  overflow: hidden;
}

.command-input-row {
  display: flex;
  align-items: center;
  gap: 0.65rem;
  padding: 0.9rem 1rem;
  border-bottom: 1px solid rgba(var(--overlay-rgb), 0.08);
}
.command-input-row svg { width: 18px; height: 18px; color: var(--text-muted); }
.command-input {
  flex: 1;
  background: transparent;
  border: none;
  outline: none;
  color: var(--text-primary);
  font-family: inherit;
  font-size: 0.95rem;
}

.command-results { list-style: none; overflow-y: auto; padding: 0.4rem; }
.command-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  width: 100%;
  padding: 0.6rem 0.75rem;
  background: none;
  border: none;
  border-radius: var(--radius-sm);
  color: var(--text-primary);
  font-family: inherit;
  font-size: 0.88rem;
  text-align: left;
  cursor: pointer;
  transition: background var(--transition-fast);
}
.command-item:hover { background: rgba(var(--overlay-rgb), 0.07); }
.command-path { color: var(--text-muted); font-size: 0.75rem; }
.command-empty { padding: 1.25rem; text-align: center; color: var(--text-muted); font-size: 0.88rem; }

@media (max-width: 768px) {
  .search-trigger { width: auto; padding: 0.5rem; }
  .search-placeholder, .search-kbd { display: none; }
  .navbar { padding: 1rem; height: 68px; }
  .navbar-actions { gap: 0.25rem; }
  .navbar-left { gap: 0.5rem; }
  /* The breadcrumb trail duplicates the page heading on small screens. */
  .navbar-breadcrumbs { display: none; }
}

@media (max-width: 576px) {
  /* Keep only the avatar; the name and role would wrap onto three lines. */
  .user-info { display: none; }
}
