<header class="navbar">
  <div class="navbar-left">
    <button class="hamburger-btn" type="button" aria-label="Toggle sidebar" (click)="toggleSidebar()">
      <svg fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>

    <button class="search-trigger" type="button" (click)="openCommandPalette()">
      <svg class="search-icon" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
      </svg>
      <span class="search-placeholder">Search or jump to…</span>
      <kbd class="search-kbd">⌘K</kbd>
    </button>

    <app-breadcrumbs class="navbar-breadcrumbs" />
  </div>

  <div class="navbar-actions">
    <button
      class="icon-btn"
      type="button"
      [attr.aria-label]="isLight() ? 'Switch to dark theme' : 'Switch to light theme'"
      (click)="toggleTheme()">
      @if (isLight()) {
        <svg fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
        </svg>
      } @else {
        <svg fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
        </svg>
      }
    </button>

    <app-dropdown [closeOnSelect]="false" panelWidth="320px">
      <button class="icon-btn" dropdown-trigger type="button" aria-label="Notifications">
        <svg fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
        </svg>
        @if (unreadCount() > 0) {
          <span class="notification-badge">{{ unreadCount() }}</span>
        }
      </button>

      <div class="notification-panel">
        <div class="notification-head">
          <span>Notifications</span>
          <button class="link-btn" type="button" (click)="markAllRead()">Mark all read</button>
        </div>
        @for (item of notifications(); track item.id) {
          <div class="notification-item" [class.unread]="item.unread">
            <p class="notification-title">{{ item.title }}</p>
            <span class="notification-time">{{ item.time }}</span>
          </div>
        } @empty {
          <p class="notification-empty">You're all caught up.</p>
        }
      </div>
    </app-dropdown>

    <app-dropdown>
      <button class="user-profile" dropdown-trigger type="button" aria-label="Account menu">
        <app-avatar [name]="profile()?.displayName ?? 'Guest'" size="sm" [status]="isAuthenticated() ? 'online' : 'offline'" />
        <span class="user-info">
          <span class="user-name">{{ profile()?.displayName ?? 'Guest' }}</span>
          <span class="user-role">{{ profile()?.role ?? 'Not signed in' }}</span>
        </span>
      </button>

      <a class="dropdown-item" routerLink="/profile">
        <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" width="16" height="16">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
        </svg>
        Profile
      </a>
      <a class="dropdown-item" routerLink="/settings">
        <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" width="16" height="16">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.066 2.573c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.573 1.066c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.066-2.573c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
        </svg>
        Settings
      </a>
      <div class="dropdown-divider"></div>
      @if (isAuthenticated()) {
        <button class="dropdown-item" type="button" (click)="logout()">
          <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" width="16" height="16">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
          </svg>
          Sign out
        </button>
      } @else {
        <a class="dropdown-item" routerLink="/auth/login">
          <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" width="16" height="16">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1" />
          </svg>
          Sign in
        </a>
      }
    </app-dropdown>
  </div>
</header>

@if (commandOpen()) {
  <div class="command-backdrop" (click)="commandOpen.set(false)">
    <div class="command-palette" role="dialog" aria-modal="true" aria-label="Command palette" (click)="$event.stopPropagation()">
      <div class="command-input-row">
        <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
        <input
          class="command-input"
          type="text"
          placeholder="Type a page name…"
          aria-label="Search pages"
          autofocus
          [value]="commandQuery()"
          (input)="onCommandQuery($event)" />
        <kbd class="search-kbd">esc</kbd>
      </div>
      <ul class="command-results">
        @for (command of commandResults(); track command.path) {
          <li>
            <button class="command-item" type="button" (click)="runCommand(command.path)">
              <span>{{ command.label }}</span>
              <span class="command-path">{{ command.group }}</span>
            </button>
          </li>
        } @empty {
          <li class="command-empty">No matches</li>
        }
      </ul>
    </div>
  </div>
}
