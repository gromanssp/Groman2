<div class="glass-panel auth-card">
  <div class="auth-header">
    <div class="logo-icon mx-auto mb-3"></div>
    <h1 class="heading-2">Create Account</h1>
    <p class="text-body text-center">Get started with Groman2 today</p>
  </div>

  @if (errorMessage()) {
    <app-alert variant="danger" class="mb-4">{{ errorMessage() }}</app-alert>
  }

  <form class="auth-form" (submit)="onSubmit(); $event.preventDefault()">
    <app-ui-input
      label="Full name"
      placeholder="Jane Doe"
      autocomplete="name"
      [required]="true"
      [field]="registerForm.fullName" />

    <app-ui-input
      label="Email address"
      type="email"
      placeholder="name@company.com"
      autocomplete="email"
      [required]="true"
      [field]="registerForm.email" />

    <app-ui-input
      label="Password"
      type="password"
      placeholder="••••••••"
      autocomplete="new-password"
      hint="Must be at least 6 characters"
      [required]="true"
      [field]="registerForm.password" />

    <app-ui-input
      label="Confirm password"
      type="password"
      placeholder="••••••••"
      autocomplete="new-password"
      [required]="true"
      [field]="registerForm.confirmPassword" />

    <app-ui-checkbox class="mb-4" label="I accept the terms of service" [field]="registerForm.acceptTerms" />

    <button type="submit" class="btn btn-gradient w-100 mb-3" [disabled]="loading()">
      {{ loading() ? 'Creating account…' : 'Create Account' }}
    </button>
  </form>

  <div class="auth-footer text-center mt-4">
    <p class="text-small">
      Already have an account?
      <a routerLink="/auth/login" class="text-primary font-medium">Log in</a>
    </p>
  </div>
</div>
