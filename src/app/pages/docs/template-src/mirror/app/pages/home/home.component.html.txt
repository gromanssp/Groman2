<div class="home-container">
  <header class="page-header">
    <div>
      <h1 class="heading-2">Dashboard Overview</h1>
      <p class="text-body">Welcome back, here is your summary for today.</p>
    </div>
    <button class="btn btn-secondary btn-sm" (click)="toggleLoading()">
      {{ loading() ? 'Show data' : 'Preview loading state' }}
    </button>
  </header>

  <section class="stats-grid">
    @for (stat of stats(); track stat.title) {
      <app-stat-card
        [title]="stat.title"
        [value]="stat.value"
        [icon]="stat.icon"
        [trend]="stat.trend"
        [accent]="stat.accent"
        [loading]="loading()" />
    }
  </section>

  <section class="charts-grid">
    <div class="glass-panel chart-container">
      <div class="chart-header">
        <h3 class="heading-3">Revenue Overview</h3>
        <span class="badge badge-primary">This Year</span>
      </div>
      <div class="chart-wrapper">
        <canvas baseChart [data]="revenueChartData()" [options]="revenueChartOptions()" type="line"></canvas>
      </div>
    </div>

    <div class="glass-panel chart-container">
      <div class="chart-header">
        <h3 class="heading-3">Active Users</h3>
        <span class="badge badge-success">Weekly</span>
      </div>
      <div class="chart-wrapper">
        <canvas baseChart [data]="usersChartData()" [options]="usersChartOptions()" type="bar"></canvas>
      </div>
    </div>
  </section>

  <section class="bottom-grid">
    <div class="glass-panel panel">
      <div class="chart-header">
        <h3 class="heading-3">Recent Orders</h3>
      </div>
      <app-data-table [columns]="orderColumns" [rows]="orders" [loading]="loading()" [striped]="true">
        <ng-template appCellTemplate="status" let-value>
          <app-badge [variant]="statusVariant(value)" size="sm">{{ value }}</app-badge>
        </ng-template>
      </app-data-table>
    </div>

    <div class="glass-panel panel">
      <div class="chart-header">
        <h3 class="heading-3">Activity</h3>
      </div>
      <app-timeline [items]="activity" />
    </div>
  </section>
</div>
