<div class="page-container">
  <header class="page-header">
    <h1 class="heading-2">Profile</h1>
    <p class="text-body">Manage your account information.</p>
  </header>

  <div class="glass-panel profile-card">
    <div class="avatar-section">
      <app-avatar [name]="profileModel().displayName || 'User'" [size]="88" status="online" />
      <h2 class="avatar-name">{{ profileModel().displayName || 'User' }}</h2>
      <p class="avatar-email">{{ profileModel().email || 'Not signed in' }}</p>
    </div>

    <app-tabs variant="pill">
      <ng-template appTab="Details">
        <form class="form-section" (submit)="saveProfile(); $event.preventDefault()">
          <app-ui-input label="Display name" placeholder="Your name" [required]="true" [field]="profileForm.displayName" />
          <app-ui-input label="Email" type="email" placeholder="your@email.com" [required]="true" [field]="profileForm.email" />
          <div class="form-actions">
            <button type="submit" class="btn btn-gradient">Save profile</button>
          </div>
        </form>
      </ng-template>

      <ng-template appTab="Security">
        <form class="form-section" (submit)="savePassword(); $event.preventDefault()">
          <app-ui-input label="Current password" type="password" [required]="true" [field]="passwordForm.current" />
          <app-ui-input
            label="New password"
            type="password"
            hint="At least 6 characters"
            [required]="true"
            [field]="passwordForm.next" />
          <app-ui-input label="Confirm new password" type="password" [required]="true" [field]="passwordForm.confirm" />
          <div class="form-actions">
            <button type="submit" class="btn btn-gradient">Update password</button>
          </div>
        </form>
      </ng-template>

      <ng-template appTab="Activity">
        <app-timeline [items]="activity" />
      </ng-template>
    </app-tabs>
  </div>
</div>
