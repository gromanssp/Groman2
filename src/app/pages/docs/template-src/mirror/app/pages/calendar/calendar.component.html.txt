<div class="page-container">
  <header class="page-header">
    <div>
      <h1 class="heading-2">Calendar</h1>
      <p class="text-body">Interactive calendar with event management.</p>
    </div>
    <button class="btn btn-gradient" (click)="openCreateModal()">+ New Event</button>
  </header>

  <div class="calendar-layout">
    <div class="glass-panel calendar-card">
      <div class="calendar-nav">
        <button class="nav-btn" (click)="shiftMonth(-1)">
          <svg fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
          </svg>
        </button>
        <h2 class="month-title">{{ monthYear() }}</h2>
        <button class="nav-btn" (click)="shiftMonth(1)">
          <svg fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </button>
      </div>

      <div class="calendar-grid">
        @for (d of weekDays; track d) {
          <div class="day-header">{{ d }}</div>
        }
        @for (week of weeks(); track $index) {
          @for (day of week; track day.year + '-' + day.month + '-' + day.date) {
            <div
              class="day-cell"
              [class.other-month]="!day.isCurrentMonth"
              [class.today]="day.isToday"
              [class.selected]="day.isSelected"
              [class.has-events]="day.events.length > 0"
              (click)="selectDay(day)">
              <span class="day-number">{{ day.date }}</span>
              @if (day.events.length > 0) {
                <div class="event-dots">
                  @for (ev of day.events; track ev.title) {
                    <span class="event-dot" [style.background]="ev.color"></span>
                  }
                </div>
              }
            </div>
          }
        }
      </div>
    </div>

    <div class="glass-panel events-panel">
      <h3 class="events-title">
        {{ selectedDate() ? (selectedDate() | date:'MMMM d, yyyy') : 'Select a date' }}
      </h3>
      @if (selectedDateEvents().length > 0) {
        <div class="events-list">
          @for (ev of selectedDateEvents(); track ev.title) {
            <div class="event-item">
              <span class="event-indicator" [style.background]="ev.color"></span>
              <span class="event-name">{{ ev.title }}</span>
            </div>
          }
        </div>
      }
      @if (selectedDate() && selectedDateEvents().length === 0) {
        <app-empty-state title="No events" description="Nothing scheduled for this day." />
      }
      @if (!selectedDate()) {
        <app-empty-state title="Select a date" description="Click on a day to see its events." />
      }
    </div>
  </div>
</div>

<app-modal title="Create Event" [(isOpen)]="showCreateModal">
  <form class="create-event-form" (submit)="createEvent(); $event.preventDefault()">
    <app-ui-input label="Event title" placeholder="Enter event title" [required]="true" [field]="eventForm.title" />
    <app-ui-input label="Date" type="date" [required]="true" [field]="eventForm.date" />
    <app-ui-select label="Color" [options]="eventColors" [field]="eventForm.color" />
  </form>

  <div modal-footer>
    <button class="btn btn-secondary" type="button" (click)="showCreateModal.set(false)">Cancel</button>
    <button class="btn btn-gradient" type="button" [disabled]="eventForm().invalid()" (click)="createEvent()">Create</button>
  </div>
</app-modal>
