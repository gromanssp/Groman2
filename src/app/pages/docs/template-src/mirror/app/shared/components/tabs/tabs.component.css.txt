:host { display: block; }

.tab-list {
  display: flex;
  gap: 0.25rem;
  overflow-x: auto;
  scrollbar-width: none;
}
.tab-list::-webkit-scrollbar { display: none; }

.tab-trigger {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.65rem 1rem;
  background: transparent;
  border: none;
  color: var(--text-secondary);
  font-family: var(--font-family);
  font-size: 0.9rem;
  font-weight: 500;
  white-space: nowrap;
  cursor: pointer;
  transition: all var(--transition-fast);
}
.tab-trigger:hover:not(:disabled) { color: var(--text-primary); }
.tab-trigger:disabled { opacity: 0.4; cursor: not-allowed; }
.tab-trigger.active { color: var(--accent-primary); }

.tab-icon { display: inline-flex; width: 16px; height: 16px; }
.tab-icon :is(svg) { width: 100%; height: 100%; }

/* Line variant */
.tabs-line .tab-list { border-bottom: 1px solid rgba(var(--overlay-rgb), 0.08); }
.tabs-line .tab-trigger { border-bottom: 2px solid transparent; margin-bottom: -1px; }
.tabs-line .tab-trigger.active { border-bottom-color: var(--accent-primary); }

/* Pill variant */
.tabs-pill .tab-list {
  padding: 0.25rem;
  background: rgba(var(--overlay-rgb), 0.04);
  border-radius: var(--radius-md);
}
.tabs-pill .tab-trigger { border-radius: var(--radius-sm); }
.tabs-pill .tab-trigger.active { background: var(--accent-gradient); color: #fff; }

.tab-panel { padding-top: 1.25rem; }
