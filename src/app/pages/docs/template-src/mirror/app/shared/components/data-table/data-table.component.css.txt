.table-wrapper {
  overflow-x: auto;
  border-radius: var(--radius-md);
  border: 1px solid rgba(var(--overlay-rgb), 0.06);
}

.data-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 0.9rem;
}

.data-table th {
  padding: 0.875rem 1rem;
  text-align: left;
  font-weight: 600;
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--text-secondary);
  background: rgba(var(--overlay-rgb), 0.03);
  border-bottom: 1px solid rgba(var(--overlay-rgb), 0.08);
}

.data-table td {
  padding: 0.75rem 1rem;
  color: var(--text-primary);
  border-bottom: 1px solid rgba(var(--overlay-rgb), 0.04);
}

.data-table tbody tr:last-child td {
  border-bottom: none;
}

.data-table.striped tbody tr:nth-child(even) {
  background: rgba(var(--overlay-rgb), 0.02);
}

.data-table.hoverable tbody tr {
  transition: background var(--transition-fast);
}

.data-table.hoverable tbody tr:hover {
  background: rgba(var(--overlay-rgb), 0.04);
}

.data-table.bordered td,
.data-table.bordered th {
  border: 1px solid rgba(var(--overlay-rgb), 0.06);
}

/* Toolbar */
.table-toolbar {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  margin-bottom: 0.9rem;
}

.table-search {
  position: relative;
  display: flex;
  align-items: center;
  flex: 1;
  max-width: 320px;
}
.table-search svg {
  position: absolute;
  left: 0.75rem;
  width: 16px;
  height: 16px;
  color: var(--text-muted);
  pointer-events: none;
}
.table-search-input {
  width: 100%;
  padding: 0.5rem 0.75rem 0.5rem 2.25rem;
  background: rgba(var(--overlay-rgb), 0.04);
  border: 1px solid rgba(var(--overlay-rgb), 0.08);
  border-radius: var(--radius-sm);
  color: var(--text-primary);
  font-family: var(--font-family);
  font-size: 0.85rem;
  transition: all var(--transition-fast);
}
.table-search-input:focus {
  outline: none;
  border-color: var(--accent-primary);
  background: rgba(var(--overlay-rgb), 0.06);
}
.table-count { font-size: 0.82rem; color: var(--text-muted); }

/* Sorting */
.data-table th.sortable { cursor: pointer; user-select: none; }
.data-table th.sortable:hover { color: var(--text-primary); }
.th-content { display: inline-flex; align-items: center; gap: 0.35rem; }

.sort-icon { display: inline-flex; width: 13px; height: 13px; opacity: 0.35; transition: opacity var(--transition-fast); }
.sort-icon.active { opacity: 1; color: var(--accent-primary); }
.sort-icon svg { width: 100%; height: 100%; }

/* Loading placeholder rows */
.cell-skeleton {
  display: block;
  height: 0.85rem;
  border-radius: var(--radius-sm);
  background: linear-gradient(
    90deg,
    rgba(var(--overlay-rgb), 0.06) 25%,
    rgba(var(--overlay-rgb), 0.12) 37%,
    rgba(var(--overlay-rgb), 0.06) 63%
  );
  background-size: 400% 100%;
  animation: app-skeleton-shimmer 1.4s ease infinite;
}

@media (prefers-reduced-motion: reduce) {
  .cell-skeleton { animation: none; }
}
