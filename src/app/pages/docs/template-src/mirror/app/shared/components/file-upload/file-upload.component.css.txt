:host { display: block; }

.dropzone {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 0.4rem;
  padding: 2rem 1.25rem;
  background: rgba(var(--overlay-rgb), 0.03);
  border: 2px dashed rgba(var(--overlay-rgb), 0.15);
  border-radius: var(--radius-md);
  color: var(--text-secondary);
  text-align: center;
  cursor: pointer;
  transition: all var(--transition-fast);
}
.dropzone:hover { border-color: var(--accent-primary); background: rgba(var(--overlay-rgb), 0.05); }
.dropzone.dragging {
  border-color: var(--accent-primary);
  background: rgba(99, 102, 241, 0.08);
  color: var(--accent-primary);
}

.dropzone-icon { width: 32px; height: 32px; color: var(--text-muted); }
.dropzone.dragging .dropzone-icon { color: var(--accent-primary); }
.dropzone-label { font-size: 0.9rem; font-weight: 500; }
.dropzone-hint { font-size: 0.78rem; color: var(--text-muted); }

.file-list { list-style: none; margin-top: 0.75rem; display: flex; flex-direction: column; gap: 0.4rem; }

.file-item {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.5rem 0.75rem;
  background: rgba(var(--overlay-rgb), 0.04);
  border: 1px solid rgba(var(--overlay-rgb), 0.08);
  border-radius: var(--radius-sm);
  font-size: 0.85rem;
}
.file-name { flex: 1; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.file-size { color: var(--text-muted); font-size: 0.78rem; }

.file-remove {
  display: flex;
  width: 15px;
  height: 15px;
  padding: 0;
  background: none;
  border: none;
  color: var(--text-muted);
  cursor: pointer;
  transition: color var(--transition-fast);
}
.file-remove:hover { color: var(--danger); }
.file-remove svg { width: 100%; height: 100%; }
